include:
    - grafana.yml
    - jaeger.yml
    - elk.yml
    - prometheus.yml
    - otel.yml

services:
    logstash:
        profiles: [ "never" ]
    kibana:
        profiles: [ "never" ]
    otel-collector:
        depends_on:
            - jaeger
            - elasticsearch
            - prometheus