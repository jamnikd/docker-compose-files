{
	"method": "{{ .method }}",
    "endpoint": "{{ .endpoint }}",
    "output_encoding": "json",
    "backend": [
        {
            "url_pattern": "{{ .url_pattern }}",
            "host": [
                "{{ .host }}"
            ]
        }
    ]
}
